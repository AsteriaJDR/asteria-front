import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-rules',
  imports: [RouterLink],
  templateUrl: './rules.html',
  styleUrl: './rules.css',
})
export class Rules {

}
